#spring.application.name=backend

# 1:

spring.application.name=backend

# Connexion a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/transporte_BD?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# En caso no quiera que se cree la tablas automatico eliminar la linea 10 y el atributo: ?createDatabaseIfNotExist=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#server.port=8080

